# ---------- Web 框架 ----------
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9          # 表单/文件上传（阶段八文档上传）
# ---------- 数据校验与配置 ----------
pydantic>=2.6
pydantic-settings>=2.0           # 配置管理（app/config.py 读 .env）
# ---------- 日志 / 重试 / HTTP ----------
loguru>=0.7
tenacity>=8.2
httpx>=0.27
# ---------- 存储层（app/storage） ----------
sqlalchemy[asyncio]>=2.0         # PostgreSQL ORM
asyncpg>=0.29                    # PG 异步驱动
redis>=5.0                       # Redis 异步客户端
minio>=7.2                       # MinIO 对象存储客户端
qdrant-client>=1.9               # Qdrant 向量库客户端（阶段四 RAG）
# ---------- 测试与代码质量 ----------
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
